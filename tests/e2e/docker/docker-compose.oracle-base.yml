services:
  os-target:
    build:
      context: ./os-target
    restart: unless-stopped
    hostname: os-target
    ports:
      - "12222:22"
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 22"]
      interval: 3s
      timeout: 3s
      retries: 10
